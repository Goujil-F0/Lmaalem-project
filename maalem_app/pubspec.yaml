name: maalem_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  provider: ^6.1.1          # Pour la gestion d'état/session
  shared_preferences: ^2.2.2 # Pour sauvegarder le token JWT
  image_picker: ^1.1.2
  dotted_border: ^2.1.0
  camera: ^0.11.0
  path_provider: ^2.1.2
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true